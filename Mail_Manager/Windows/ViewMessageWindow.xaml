<Window x:Class="MailKit_usage.Windows.ViewMessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message" Height="520" Width="780" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="From:" Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox x:Name="txtFrom" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" IsReadOnly="True"/>

        <TextBlock Text="To:" Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox x:Name="txtTo" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" IsReadOnly="True"/>

        <TextBlock Text="Subject:" Grid.Row="2" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox x:Name="txtSubject" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" IsReadOnly="True"/>

        <TextBlock Text="Date:" Grid.Row="3" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox x:Name="txtDate" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8" IsReadOnly="True"/>

        <TextBox x:Name="txtBody" Grid.Row="4" Grid.ColumnSpan="2" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" IsReadOnly="True"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Reply" Width="90" Margin="0,0,8,0" Click="BtnReply_Click"/>
            <Button Content="Close" Width="90" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>