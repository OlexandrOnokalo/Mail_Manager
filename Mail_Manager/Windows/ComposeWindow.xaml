<Window x:Class="Mail_Manager.Windows.ComposeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Compose" Height="520" Width="780" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="To:" Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox x:Name="txtTo" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Text="Subject:" Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox x:Name="txtSubject" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBox x:Name="txtBody" Grid.Row="2" Grid.ColumnSpan="2" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Attach files" Width="110" Margin="0,0,8,0" Click="BtnAttach_Click"/>
            <Button Content="Send" Width="90" Margin="0,0,8,0" Click="BtnSend_Click"/>
            <Button Content="Cancel" Width="90" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>