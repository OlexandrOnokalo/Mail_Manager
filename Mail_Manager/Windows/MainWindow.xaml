<Window x:Class="Mail_Manager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mail_Manager"
        mc:Ignorable="d"
        Title="MainWindow" Height="950" Width="1300"
        Style="{StaticResource MaterialDesignWindow}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="8">
            <Button Content="New" Width="80" Margin="0,0,8,0" Click="BtnNew_Click"/>
            <Button Content="Open" Width="80" Margin="0,0,8,0" Click="BtnOpen_Click"/>
            <Button Content="Delete" Width="80" Margin="0,0,8,0" Click="BtnDelete_Click"/>
            <TextBlock Text="Current Folder:" Margin="16,0,8,0" VerticalAlignment="Center"/>
            <TextBlock x:Name="txtCurrentFolder" VerticalAlignment="Center" FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal" Margin="24,0,0,0">
                <TextBlock Text="Page:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBlock x:Name="txtPage" VerticalAlignment="Center" FontWeight="Bold"/>
            </StackPanel>
        </StackPanel>


        <GroupBox Header="Folders" Grid.Row="1" Grid.Column="0" Margin="8">
            <ListBox x:Name="lstFolders" DisplayMemberPath="FullName" SelectionChanged="LstFolders_SelectionChanged"/>
        </GroupBox>


        <GroupBox Header="Messages" Grid.Row="1" Grid.Column="1" Margin="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListView x:Name="lstMessages" Grid.Row="0" SelectionMode="Single">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="From" Width="260" DisplayMemberBinding="{Binding From}"/>
                            <GridViewColumn Header="Subject" Width="460" DisplayMemberBinding="{Binding Subject}"/>
                            <GridViewColumn Header="Date" Width="180" DisplayMemberBinding="{Binding Date}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="Prev" Width="80" Margin="0,0,8,0" Click="BtnPrev_Click"/>
                    <Button Content="Next" Width="80" Click="BtnNext_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>


        <StatusBar Grid.Row="2" Grid.ColumnSpan="2">
            <StatusBarItem Content="Ready" />
        </StatusBar>
    </Grid>
</Window>
