<Window x:Class="Mail_Manager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mail_Manager"
        mc:Ignorable="d"
        Title="MainWindow" Height="650" Width="950"
        Style="{StaticResource MaterialDesignWindow}">
    <Grid Width="850" Height="600">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition  Width="100"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="20"/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Button Grid.Column="1" Grid.Row="1" Padding="30 5" Margin="0 0 5 0" Click="BtnNew_Click" >New</Button>
        <Button Grid.Column="2" Grid.Row="1" Padding="30 5" Margin="5 0">Open</Button>
        <Button Grid.Column="3" Grid.Row="1" Padding="30 5" Margin="5 0" Grid.ColumnSpan="2" >Delete</Button>
        <ListBox Grid.Column="1" Grid.Row="3" Grid.ColumnSpan="3" Grid.RowSpan="2" Background="AliceBlue" Name="FolderBox" SelectionChanged="FolderBox_SelectionChanged" Foreground="Black"></ListBox>
        <ListBox Grid.Column="5" Grid.Row="3" Grid.ColumnSpan="7" Grid.RowSpan="2" Background="AliceBlue" Name="MsgBox" ItemsSource="{Binding}">
            <ListBox.ItemTemplate>
            <DataTemplate>
                <StackPanel>
                        <TextBlock Text="{Binding Date}"  Foreground="Black">
                        </TextBlock>
                        <TextBlock Text="{Binding Subject}" FontWeight="Bold" Foreground="Black">
                    </TextBlock>
                </StackPanel>
            </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Grid.Column="10" Grid.Row="6" Margin="5 0">Preview</Button>
        <Button Grid.Column="11" Grid.Row="6"  Margin="5 0 0 0">Next</Button>
        <Label Grid.Column="6" Grid.Row="1">Current Folder:</Label>
        <Label Grid.Column="7" Grid.Row="1"></Label> <!-- Current folder-->
        <Label Grid.Column="8" Grid.Row="1">Page:</Label>
        <Label Grid.Column="9" Grid.Row="1"></Label> <!-- Page number -->

    </Grid>
</Window>
